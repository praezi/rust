sudo: false
language: rust

rust:
  - stable
  - nightly

script: |
  cargo build --bin ufi --verbose &&
  cargo build --bin launch --verbose
  
matrix:
  allow_failures:
    - rust: nightly
  include:
    - rustup component add rustfmt-preview
      script:
        - cargo fmt --all -- --check
